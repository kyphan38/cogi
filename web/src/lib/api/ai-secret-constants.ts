/** localStorage key — must match login page after successful verify. */
export const AI_SECRET_STORAGE_KEY = "my_secret";

/** Request header checked by middleware and matched to `APP_API_SECRET`. */
export const AI_SECRET_HEADER = "X-App-Api-Secret";
